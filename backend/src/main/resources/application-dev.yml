spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DB_NAME:expense_tracker}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: ${DB_USER:expense_app}
    password: ${DB_PASSWORD:change-me-app}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.example.expense: debug
    com.baomidou.mybatisplus: info

