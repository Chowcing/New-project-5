<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.expense.statistics.mapper.StatisticsMapper">
    <select id="selectMonthlyTotals" resultType="com.example.expense.statistics.dto.MonthlyTotals">
        SELECT
            COALESCE(SUM(CASE WHEN type = 'EXPENSE' THEN amount ELSE 0 END), 0) AS total_expense,
            COALESCE(SUM(CASE WHEN type = 'INCOME' THEN amount ELSE 0 END), 0) AS total_income,
            COUNT(*) AS transaction_count,
            COUNT(CASE WHEN type = 'EXPENSE' THEN 1 END) AS expense_count,
            COUNT(CASE WHEN type = 'INCOME' THEN 1 END) AS income_count
        FROM transactions
        WHERE user_id = #{userId}
          AND deleted = 0
          AND occurred_at &gt;= #{startAt}
          AND occurred_at &lt; #{endAt}
    </select>

    <select id="selectCategorySummary" resultType="com.example.expense.statistics.dto.CategorySummary">
        SELECT
            t.category_id,
            COALESCE(c.name, '已删除分类') AS category_name,
            COALESCE(SUM(t.amount), 0) AS amount,
            COUNT(*) AS transaction_count
        FROM transactions t
        LEFT JOIN categories c ON c.id = t.category_id AND c.user_id = t.user_id
        WHERE t.user_id = #{userId}
          AND t.deleted = 0
          AND t.type = #{type}
          AND t.occurred_at &gt;= #{startAt}
          AND t.occurred_at &lt; #{endAt}
        GROUP BY t.category_id, c.name
        ORDER BY amount DESC
    </select>

    <select id="selectDailySummary" resultType="com.example.expense.statistics.dto.DailySummary">
        SELECT
            DATE_FORMAT(t.occurred_at, '%Y-%m-%d') AS date,
            COALESCE(SUM(CASE WHEN t.type = 'EXPENSE' THEN t.amount ELSE 0 END), 0) AS total_expense,
            COALESCE(SUM(CASE WHEN t.type = 'INCOME' THEN t.amount ELSE 0 END), 0) AS total_income,
            COUNT(*) AS transaction_count
        FROM transactions t
        WHERE t.user_id = #{userId}
          AND t.deleted = 0
          AND t.occurred_at &gt;= #{startAt}
          AND t.occurred_at &lt; #{endAt}
        GROUP BY DATE_FORMAT(t.occurred_at, '%Y-%m-%d')
        ORDER BY date ASC
    </select>

    <select id="selectExpenseByChannel" resultType="com.example.expense.statistics.dto.ChannelSummary">
        SELECT
            t.channel,
            COALESCE(SUM(t.amount), 0) AS amount,
            COUNT(*) AS transaction_count
        FROM transactions t
        WHERE t.user_id = #{userId}
          AND t.deleted = 0
          AND t.type = 'EXPENSE'
          AND t.occurred_at &gt;= #{startAt}
          AND t.occurred_at &lt; #{endAt}
        GROUP BY t.channel
        ORDER BY amount DESC
    </select>

    <select id="selectExpenseByPaymentMethod" resultType="com.example.expense.statistics.dto.PaymentMethodSummary">
        SELECT
            t.payment_method_id,
            COALESCE(pm.name, t.payment_method_name, '已删除支付方式') AS payment_method_name,
            COALESCE(SUM(t.amount), 0) AS amount,
            COUNT(*) AS transaction_count
        FROM transactions t
        LEFT JOIN payment_methods pm
            ON pm.id = t.payment_method_id
            AND pm.user_id = t.user_id
            AND pm.deleted = 0
        WHERE t.user_id = #{userId}
          AND t.deleted = 0
          AND t.type = 'EXPENSE'
          AND t.occurred_at &gt;= #{startAt}
          AND t.occurred_at &lt; #{endAt}
        GROUP BY t.payment_method_id, COALESCE(pm.name, t.payment_method_name, '已删除支付方式')
        ORDER BY amount DESC
    </select>
</mapper>
